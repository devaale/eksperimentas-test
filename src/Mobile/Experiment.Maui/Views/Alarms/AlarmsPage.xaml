<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="Experiment.Maui.Views.Alarms.AlarmsPage"
			 xmlns:VM="clr-namespace:Experiment.Maui.ViewModels.Alarms"
			 Title="{Binding Title}">
	<ContentPage.BindingContext>
		<VM:AlarmsViewModel />
	</ContentPage.BindingContext>
	<ContentPage.Content>
		<StackLayout>

			<ScrollView>
				<ListView x:Name="LvwItems"
						  ItemsSource="{Binding Items, Mode=OneWay}"
						  SelectedItem="{Binding SelectedItem}"
						  HasUnevenRows="True"
						  SelectionMode="Single"
						  IsPullToRefreshEnabled="True"
						  RefreshCommand="{Binding RefreshCommand}"
						  IsRefreshing="{Binding IsRefreshing}">

					<ListView.ItemTemplate>
						<DataTemplate>
							<!--
							<SwitchCell Text="{Binding VisualName, Mode=OneTime}"
										On="{Binding VisualState, Mode=OneTime}" />
							-->
							<ViewCell>
								<ViewCell.View>

									<Grid ColumnSpacing="0"
										  RowSpacing="0">

										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="24" />
											<ColumnDefinition Width="*" />
										</Grid.ColumnDefinitions>
										<Grid.RowDefinitions>
											<RowDefinition Height="Auto" />
											<RowDefinition Height="Auto" />
										</Grid.RowDefinitions>

										<Frame Grid.Column="0"
											   Grid.RowSpan="2"
											   Margin="4"
											   Style="{Binding ActiveStateStyle}">
										</Frame>

										<Label Grid.Column="1"
											   Grid.Row="0"
											   Text="{Binding Name}"
											   FontAttributes="Bold" />

										<Label Grid.Column="1"
											   Grid.Row="1"
											   Text="{Binding EventTime}"
											   FontSize="Caption"/>
									</Grid>
								</ViewCell.View>
							</ViewCell>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</ScrollView>

			<ActivityIndicator IsVisible="{Binding IsBusy}"
							   IsRunning="{Binding IsBusy}" />
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
